export interface ProjectOption {
  id: string;
  name: string;
}

export interface QuickTrackFormValues {
  projectId: string;
  trackingDate: string;
  durationHours: number;
  description: string;
}
